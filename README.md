# Yarn private registry test

Simple test to see if Yarn passes the correct token from npmrc to a registry

## Running

In one terminal:

```
git clone https://github.com/rexxars/yarn-private.git
cd yarn-private
npm install
npm start
```

In a different terminal:

```
cd yarn-private/target
yarn
```

